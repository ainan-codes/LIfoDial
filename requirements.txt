# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.40
aiosqlite==0.21.0
asyncpg==0.30.0
alembic==1.15.2
psycopg[binary]>=3.1.0

# ── HTTP client (all external API calls) ─────────────────────────────────────
httpx==0.27.2
aiohttp>=3.9.0          # Required by pipecat Sarvam TTS HTTP service

# ── Config & auth ─────────────────────────────────────────────────────────────
python-dotenv==1.1.0
pydantic-settings==2.9.1
python-jose[cryptography]==3.4.0
python-multipart==0.0.20

# ── AI / LLM providers ────────────────────────────────────────────────────────
google-genai>=1.0.0
openai>=1.0.0              # Used by pipecat LLM context (universal format) + Whisper fallback

# ── Pipecat — voice agent framework ──────────────────────────────────────────
# BSD-2-Clause open source. Zero licensing cost.
# [sarvam]  → SarvamSTTService + SarvamTTSService (first-party, native Indian language support)
# [livekit] → LiveKitTransport (WebRTC transport)
# [google]  → GoogleLLMService (Gemini 2.0 Flash streaming)
pipecat-ai[sarvam,livekit,google]>=0.0.105

# ── LiveKit (kept for FastAPI token generation in routers/agents.py) ─────────
# NOTE: livekit-agents SDK is replaced by pipecat. Only livekit-api is needed.
livekit-api>=0.7.0

# ── Telephony / billing ───────────────────────────────────────────────────────
twilio==9.4.4
stripe==11.5.0

# ── Analytics & monitoring ───────────────────────────────────────────────────
posthog==3.8.3

# ── WebSocket ─────────────────────────────────────────────────────────────────
websockets>=12.0
