# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.40
aiosqlite==0.21.0
asyncpg==0.30.0
alembic==1.15.2
psycopg[binary]>=3.1.0

# ── HTTP client ───────────────────────────────────────────────────────────────
# >=0.28.1 required by the agent worker's pipecat[google] (google-genai needs
# httpx>=0.28.1); the old ==0.27.2 pin made the agent Docker build fail with
# ResolutionImpossible. Backend is verified working on 0.28.1 locally and uses
# only plain AsyncClient(timeout=...) (no app=/proxies= that 0.28 changed).
httpx>=0.28.1,<1.0
aiohttp>=3.9.0

# ── Config & auth ─────────────────────────────────────────────────────────────
python-dotenv==1.1.0
pydantic-settings==2.9.1
python-jose[cryptography]==3.4.0
python-multipart==0.0.20

# ── AI / LLM providers (FastAPI side — lightweight SDK only) ──────────────────
google-genai>=1.0.0
openai>=1.0.0
groq>=0.9.0

# ── LiveKit API (token generation for FastAPI routers ONLY) ──────────────────
# NOTE: Full LiveKit RTC + Pipecat agent deps are in requirements.agent.txt
livekit-api>=0.7.0

# ── Telephony / billing ───────────────────────────────────────────────────────
twilio==9.4.4
stripe==11.5.0

# ── Analytics & monitoring ───────────────────────────────────────────────────
posthog==3.8.3
# Error monitoring. Wired in main.py only when SENTRY_DSN is set (audit O1:
# previously declared in render.yaml but never installed or initialized).
sentry-sdk[fastapi]>=2.20.0,<3.0

# ── WebSocket ─────────────────────────────────────────────────────────────────
websockets>=12.0

# ── Image processing (widget avatar resize/optimize) ─────────────────────────
Pillow>=10.0.0
